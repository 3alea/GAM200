#include "AEXGameState.h"
#include <list>
#include "../Composition/AEXObjectManager.h"

namespace AEX
{
	/*json & IGameState::operator<<(json & j) const
	{
		return ObjectManager::Instance()->operator<<(j);
	}

	void IGameState::operator >> (json & j)
	{
		ObjectManager::Instance()->operator >> (j);
	}

	std::ostream & IGameState::operator<<(std::ostream & o) const
	{
		json j;
		this->operator<<(j);
		return o << j;
	}*/

}
